<UserControl x:Class="Isis.CustomControls.DataLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="455" d:DesignWidth="800"
             
             DataContext="{Binding DataLogView,Source={StaticResource Locator}}">
    <Grid>
        <Button Content="OpenLog" HorizontalAlignment="Left" VerticalAlignment="Top" Width="100" Margin="10,10,0,0" Command="{Binding MyCommand}"/>
        <DataGrid AutoGenerateColumns="False" HorizontalAlignment="Left" Margin="10,125,0,0" VerticalAlignment="Top" Width="780" Height="320" ItemsSource="{Binding BatteryDataItem.BatteryData.ParsedDataCollection}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PackSocProp.SocAmpHours}">
                    
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
